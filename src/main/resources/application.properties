# Kết nối MySQL (Tự động tạo DB nếu chưa có)
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:pbl3_management}?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:NexToue257@}

# Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Cấu hình Thymeleaf
spring.thymeleaf.cache=false

#MaiSender Config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=furidom4everalone@gmail.com
spring.mail.password=AIcomputerapp7@
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cấu hình server
server.port=8080
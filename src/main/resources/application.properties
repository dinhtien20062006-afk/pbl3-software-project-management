# Kết nối MySQL (Tự động tạo DB nếu chưa có)
spring.datasource.url=jdbc:mysql://localhost:3306/pbl3_management?createDatabaseIfNotExist=true&useSSL=false&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Tien2006@

# Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Cấu hình Thymeleaf
spring.thymeleaf.cache=false

# Cấu hình server
server.port=8080